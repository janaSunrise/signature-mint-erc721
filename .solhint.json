{
  "extends": "solhint:recommended",
  "plugins": ["prettier"],
  "rules": {
    "func-visibility": [
      "error",
      {
        "ignoreConstructors": true
      }
    ],
    "compiler-version": ["error", "^0.8.0"],
    "prettier/prettier": [
      "error",
      {
        "endOfLine": "auto"
      }
    ]
  }
}
